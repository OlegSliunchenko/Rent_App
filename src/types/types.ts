export type ContainerType = {
    divStyle: string;
    children: any;
};
export type AppartmentType = {
    title: string;
    address: string;
    price: number;
    rooms: number;
};
export type ButtonType = {
    divStyle: string;
    name: string;
    onClick: () => void;

};
