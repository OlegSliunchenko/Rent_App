export type ContainerType = {
    divStyle: string;
    children: any;
};
export type ApartmentType = {
    title: string;
    address: string;
    price: number;
    rooms: number;
};
export type ButtonType = {
    divStyle: string;
    name: string;
    handler: () => void;

};
